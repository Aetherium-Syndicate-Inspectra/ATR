# ATR Core Invariants Index

Canonical policy source: `AGENTS.md`.

This file is a navigational index for contributors and reviewers.

## Invariant Domains

1. Determinism invariants
2. Envelope validation and immutability invariants
3. E3 truth model invariants (log + snapshot)
4. Delivery semantics invariants (effectively-once)
5. Failure-mode invariants (predictable degradation + recovery)
6. Security invariants
7. Performance invariants (benchmark contract)

For normative rules and precedence, always follow `AGENTS.md`.
